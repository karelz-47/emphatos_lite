# ── Core ──────────────────────────────
streamlit==1.46.1          # last stable, wheels for py39-311
openai==1.28.1

# ── Helpers ───────────────────────────
python-dotenv>=1.0,<2.0

# ── LLM / tokeniser ───────────────────
tiktoken==0.8.0            # wheels for py39-311

# ── Streamlit transitive deps needing wheels ──
pyarrow==16.1.0            # first version with py311 wheels
watchdog>=4,<5             # 4.x wheels cover py39-311

# ── General safety pins ───────────────
pydantic>=2.7,<3.0
requests>=2.32,<3.0
