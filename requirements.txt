# ── Core ───────────────────────────
streamlit==1.45.2        # still ships cp39/310/311 wheels
openai==1.30.1

# ── Helpers ────────────────────────
python-dotenv>=1.0,<2.0  # local .env support

# ── LLM-related ────────────────────
tiktoken==0.6.0          # wheels for cp39-cp311, no Rust build needed

# ── Streamlit deps needing wheels ──
pyarrow==15.0.2          # last version with small wheels for all Py3
watchdog>=4,<5           # 4.x ships wheels

# ── General safety pins ────────────
pydantic>=2.7,<3.0
requests>=2.32,<3.0
